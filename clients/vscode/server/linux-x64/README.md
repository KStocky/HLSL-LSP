# Future Linux x64 bundle

A future Linux x64 package will stage `hlsl-lsp`, `libdxcompiler.so`, and any
other required runtime files here. The current VSIX deliberately contains no
Linux executable; configure `hlsl.server.path` to use an external build.

# Linux x64 bundle

`npm run stage:runtime -- --platform linux-x64 --server-dir <directory>`
stages `hlsl-lsp` and `libdxcompiler.so` here. Generated runtime files are
ignored by Git and included in the packaged VSIX.
